/**
 * Shared motion feel for Generation Bread (customer-facing bakery).
 * Warm, calm, readable — not flashy or “tech demo”.
 */
export const GB_EASE = {
  out: "power2.out",
  inOut: "power2.inOut",
  soft: "sine.out",
} as const;
